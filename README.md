# 📝 Todo App (Lesson 69)

**Todo App** — это мобильное приложение для управления задачами, разработанное с использованием принципов «Чистой архитектуры» (Clean Architecture). Проект служит отличным примером построения масштабируемых приложений на Flutter с разделением на слои и использованием современных библиотек.

## ✨ Основные возможности

* **📋 Управление задачами**: Полный цикл CRUD (Создание, Чтение, Обновление, Удаление) для задач.
* **💾 Локальная база данных**: Надежное хранение данных на устройстве с использованием **SQLite** (через ORM Drift).
* **🔄 Реактивный UI**: Мгновенное обновление интерфейса при изменении данных.
* **🧭 Навигация**: Удобные переходы между экранами списка, создания и редактирования.

## 🛠 Технологический стек

* **Фреймворк**: [Flutter](https://flutter.dev/) (Dart)
* **База данных**: [Drift](https://drift.simonbinder.eu/) — производительная и типобезопасная ORM для SQLite.
* **Управление состоянием**: [Riverpod](https://riverpod.dev/) — для управления состоянием и связывания бизнес-логики с UI.
* **Внедрение зависимостей (DI)**: Реализован собственный механизм DI (`RootDependencyContainer`, `RootDependencyFactory`) для управления зависимостями.
* **Навигация**: [GoRouter](https://pub.dev/packages/go_router) — декларативная маршрутизация.

## 📂 Архитектура проекта

Проект организован по принципу разделения на **Core** (ядро) и **Features** (функциональность):

```text
lib/
├── core/
│   ├── database/         # Конфигурация Drift БД и таблицы
│   ├── dependencies/     # Внедрение зависимостей (DI Container)
│   └── navigator/        # Настройка маршрутов (GoRouter)
├── feature/
│   └── todo/             # Модуль задач
│       ├── model/        # Слой данных
│       │   ├── database/     # Описание таблиц (TodoTable)
│       │   └── repositories/ # Репозитории (TodoRepository)
│       ├── view/         # Слой представления (UI)
│       │   ├── screens/      # Экраны (Create, Update, List)
│       │   └── widgets/      # Виджеты
│       └── viewmodel/    # Слой логики
│           ├── controllers/  # Notifiers (управление состоянием)
│           └── providers/    # Riverpod провайдеры
└── main.dart             # Точка входа
